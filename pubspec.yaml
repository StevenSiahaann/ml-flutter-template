name: submission
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  camera: ^0.12.0+1
  firebase_core: ^4.11.0
  firebase_ml_model_downloader: ^0.4.2+4
  firebase_storage: ^13.4.3
  flutter:
    sdk: flutter

  flutter_dotenv: ^6.0.1
  google_generative_ai: ^0.4.7
  http: ^1.6.0
  image: ^4.9.1
  image_cropper: ^12.2.1
  image_picker: ^1.2.3
  path_provider: ^2.1.6
  permission_handler: ^12.0.3
  provider: ^6.1.5+1
  tflite_flutter: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
